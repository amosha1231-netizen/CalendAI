{
  "name": "calendai-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios",
    "cap:build": "vite build && npx cap copy",
    "cap:build:android": "vite build && npx cap copy && npx cap sync android",
    "cap:build:ios": "vite build && npx cap copy && npx cap sync ios"
  },
  "dependencies": {
    "@hebcal/core": "^6.9.2",
    "axios": "^1.19.0",
    "lucide-react": "^0.436.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.41",
    "tailwindcss": "^3.4.10",
    "vite": "^5.4.10"
  }
}